export enum Status {
  Active = "Active",
  Delete = "Delete",
}

export enum AdminRoleType {
  Admin = "Admin",
  Agent = "Agent",
}

export enum UserTargetType {
  Individual = "Individual",
  Company = "Company",
}

export enum UserStatus {
  Inactive = "Inactive",
  Actived = "Active",
  Blocked = "BLocked",
}
